<div class="page-width">
      <div class="section-header text-center">
        <h1>{{ page.title }}</h1>
      </div>

      <div class="rte">
        <div class="timeline">
          {% section 'history' %}
        </div>
        <span class='gc_autolink_page'>{{ page.content }}</span>
      </div>
</div>
